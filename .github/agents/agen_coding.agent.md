# example: activate venv
source venv/bin/activate

# or if your env is named .venv
source .venv/bin/activate

# install required packages into the active venv
pip install Flask Flask-SQLAlchemy Flask-Login Authlib SQLAlchemy python-dotenv